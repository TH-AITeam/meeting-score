# AGENT.md

## 目的

このプロジェクトは、会議中の各発言について「会議をどれだけ前進させたか」を複数軸で評価し、会議改善・振り返りに使える形で可視化する MVP を実装するためのものである。

このプロジェクトの評価対象は**人の優秀さそのものではなく、会議への実質的な貢献**である。人事査定用途を前提にしてはならない。

---

# 1. プロダクトの前提

## ゴール

会議ログを入力として受け取り、各発言に対して以下を出力する。

* 発言タイプ
* 軸別スコア
* 減点判定
* 総合貢献スコア
* 理由

さらに、会議全体・話者単位で集計し、以下を表示する。

* 会議を前進させた発言 Top 5
* 論点整理に貢献した発言
* 意思決定を進めた発言
* リスクを可視化した発言
* アクション化した発言
* 話者別サマリー
* 会議改善コメント

## 非ゴール

以下は MVP の範囲外とする。

* 人事評価
* 昇進査定連携
* リアルタイム会議アシスト
* 高度な教師あり学習
* 全会議タイプへの汎化

---

# 2. 最重要原則

実装時は以下を必ず守ること。

1. **発言量は加点しない**
2. **長く話しただけでは高評価にしない**
3. **短くても会議を動かした発言を高く評価する**
4. **肩書きや立場で補正しない**
5. **既出内容の繰り返しは減点する**
6. **会議目的への寄与で評価する**
7. **UI や表現で「人の優秀さランキング」に見せない**

---

# 3. 対象会議

MVP では最初に以下のいずれか1種類に限定すること。

* 企画会議
* 開発定例
* 要件定義会議

初期推奨は **企画会議** または **開発定例**。

ブレストや 1on1 は後回しにすること。

---

# 4. 入力仕様

## 必須入力

* 会議タイトル
* 会議目的
* 発言列（発言単位に分割済み）
* 話者
* タイムスタンプ

## 推奨入力

* アジェンダ
* 決めるべき事項
* 現在の議題
* 会議終了時の決定事項

## 入力 JSON の最小形

```json
{
  "meeting_id": "m001",
  "title": "新機能企画会議",
  "goal": "初回リリース範囲を決める",
  "agenda": ["対象ユーザー確認", "初回リリース範囲整理", "スケジュール確認"],
  "decision_points": ["初回に含める機能", "見積もり前提条件"],
  "utterances": [
    {
      "utterance_id": "u001",
      "speaker": "A",
      "timestamp": "00:12:31",
      "text": "今決めるべきはUI仕様ではなく、今月中に出す範囲ですよね。"
    }
  ]
}
```

---

# 5. 出力仕様

各発言について以下を返すこと。

* `speech_type`
* `scores`
* `penalties`
* `total_score`
* `reason`

## 出力 JSON の最小形

```json
{
  "utterance_id": "u001",
  "speaker": "A",
  "timestamp": "00:12:31",
  "text": "今決めるべきはUI仕様ではなく、今月中に出す範囲ですよね。",
  "speech_type": "論点整理",
  "scores": {
    "issue_clarification": 3,
    "decision_progress": 2,
    "risk_detection": 0,
    "actionability": 0,
    "groundedness": 1,
    "novelty": 2,
    "summarization": 1
  },
  "penalties": {
    "duplication": 0,
    "verbosity": 0,
    "off_topic": 0,
    "unsupported_assertion": 0
  },
  "total_score": 6.6,
  "reason": "議論の焦点をUI詳細からリリース範囲に戻し、意思決定に必要な論点を明確にした。"
}
```

---

# 6. 評価軸

## 主評価軸

以下を MVP の中核とする。

### 6.1 論点整理

議論のズレを戻したり、考えるべき論点を明確にした度合い。

### 6.2 意思決定寄与

選択肢比較、判断材料提示、結論への前進にどれだけ効いたか。

### 6.3 リスク検知

抜け漏れ、依存関係、障害要因、後で事故る点を見つけた度合い。

### 6.4 アクション化

議論を次の具体行動に落とし込んだ度合い。

## 補助評価軸

* 根拠性
* 新規性 / 洞察
* 要約・交通整理

## 減点軸

* 重複
* 冗長
* 論点逸脱
* 根拠薄い断言

---

# 7. 発言タイプ分類

以下のラベルを使用すること。

* 論点整理
* 提案
* 質問
* 情報共有
* 要約
* 懸念提示
* 根拠提示
* 意思決定促進
* 雑談 / 脱線

---

# 8. スコアリング仕様

## 軸スコア

主評価軸・補助評価軸は 0〜3 点で採点する。

* 0: 該当なし
* 1: 軽く寄与
* 2: 明確に寄与
* 3: 強く寄与

## 減点軸

減点軸は 0〜-3 で採点する。

* 0: 問題なし
* -1: 軽微
* -2: 明確
* -3: 強い問題

## 総合点

```text
総合点 =
論点整理 × 1.3
+ 意思決定寄与 × 1.5
+ リスク検知 × 1.2
+ アクション化 × 1.3
+ 根拠性 × 0.8
+ 新規性 × 0.9
+ 要約・交通整理 × 0.8
+ 重複
+ 冗長
+ 論点逸脱
+ 根拠薄い断言
```

### 実装上の注意

* 重みは定数ファイルに切り出すこと
* 後で設定変更できるようにすること
* MVP では絶対値より順位の納得感を優先すること

---

# 9. 文脈評価ルール

対象発言だけを見て採点してはならない。
必ず以下を入力として使うこと。

* 会議目的
* 現在の議題
* 直前3発言
* 対象発言
* 直後3発言

理由:

* 単なる繰り返しかどうかは単体ではわからない
* 発言後に議論が収束したかを見たい
* 本当に論点整理や意思決定寄与だったかを判断する必要がある

---

# 10. LLM 評価の役割

LLM は以下だけを担当させること。

1. 発言タイプ分類
2. 軸別採点
3. 減点判定
4. 理由生成

## 判定原則

* 発言のうまさではなく、会議目的への寄与で見る
* 短くても会議を動かした発言は高評価
* 長くても新規性や進展がなければ高評価しない
* 既出内容の言い換えは重複として減点
* 肩書きによる補正はしない
* 理由は具体的に短く返す

## プロンプト雛形

`prompts/utterance_eval.txt` を作成し、以下方針で実装すること。

```text
あなたは会議分析アシスタントです。
目的は、各発言が会議をどれだけ前進させたかを評価することです。
話し方の上手さや肩書きではなく、会議目的への実質的な貢献で採点してください。

入力:
- 会議目的
- 現在の議題
- 直前の発言群
- 対象発言
- 直後の発言群

次の項目をJSONで返してください:
1. 発言タイプ
2. 論点整理: 0-3
3. 意思決定寄与: 0-3
4. リスク検知: 0-3
5. アクション化: 0-3
6. 根拠性: 0-3
7. 新規性: 0-3
8. 要約・交通整理: 0-3
9. 重複: 0〜-3
10. 冗長: 0〜-3
11. 論点逸脱: 0〜-3
12. 根拠薄い断言: 0〜-3
13. reason: 日本語1-2文
```

---

# 11. 実装アーキテクチャ

推奨構成は以下。

```text
project/
  app/
    ingest/
    context_builder/
    evaluators/
    scoring/
    aggregation/
    reporting/
    schemas/
    api/
    ui/
  prompts/
    utterance_eval.txt
  data/
    sample_meetings/
  tests/
  docs/
```

## モジュール責務

* `ingest`: 入力正規化
* `context_builder`: 前後文脈生成
* `evaluators`: LLM 評価
* `scoring`: 総合点計算
* `aggregation`: 話者別・会議別集計
* `reporting`: 画面用データ整形
* `schemas`: データモデル定義
* `api`: バックエンド API
* `ui`: 最低限の可視化 UI

---

# 12. エージェント実装手順

以下の順で実装すること。

## Step 1: スキーマ定義

最低限以下を定義すること。

* `MeetingInput`
* `Utterance`
* `EvaluatedUtterance`
* `SpeakerSummary`
* `MeetingSummary`

推奨: Pydantic

## Step 2: 入力正規化

* JSON を読み込む
* 発言順を保証する
* 欠損値を補正する

## Step 3: 文脈ウィンドウ生成

* 各発言に対して前後3発言を付与する
* 会議目的と議題を埋め込む

## Step 4: LLM 評価器実装

* プロンプトテンプレート読み込み
* JSON 出力を要求
* パース失敗時のリトライ実装
* 失敗時は安全なデフォルト値を返す

## Step 5: スコア計算

* 軸別点数と減点を統合して総合点を算出する
* 重みは設定ファイル管理にする

## Step 6: 集計処理

* 話者別平均
* 代表発言抽出
* 会議内 Top 発言抽出
* タグ別抽出

## Step 7: レポート整形

* 発言一覧
* 会議サマリー
* 話者サマリー
  を UI が扱いやすい形式にする

## Step 8: UI 実装

最低限以下の3画面を作ること。

* 会議サマリー
* 発言タイムライン
* 話者別分析

## Step 9: サンプル検証

* サンプル会議ログで実行
* 結果を人が見て点検
* 重みと表示文言を調整

---

# 13. UI の見せ方

以下の表現を優先すること。

* 会議を前進させた発言
* 論点を整理した発言
* 意思決定を進めた発言
* リスクを可視化した発言
* 次アクションを明確にした発言

以下の表現は避けること。

* 優秀度ランキング
* 社員ランキング
* 発言力ランキング

理由: 人物評価に見えやすく、用途がブレるため。

---

# 14. 受け入れ条件

MVP 完了条件は以下。

* 文字起こし JSON 1本を入力できる
* 各発言について `speech_type`, `scores`, `penalties`, `total_score`, `reason` を出せる
* 会議を前進させた発言 Top 5 を表示できる
* 話者別サマリーを表示できる
* 重複・冗長・脱線の最低限の検出ができる
* 人が見て明らかに破綻していない

---

# 15. 検証基準

以下を必ず確認すること。

* 明らかに良い発言が上位に来るか
* 長話マンが勝っていないか
* 要約・整理役がきちんと評価されるか
* 重複発言が下がるか
* 人間が見て「まあ納得」できるか

---

# 16. 実装上の注意

## やってはいけないこと

* 発言数で加点する
* 発話文字数だけで高得点をつける
* 役職者を暗黙に優遇する

## 優先すべきこと

* 順位の納得感
* 低コストで動く最小構成
* 後からルーブリックと重みを差し替えられる構造
* プロンプトとロジックの分離

---

# 17. まず最初に作るもの

実装開始時は、まず以下を揃えること。

1. サンプル会議ログ 1〜3 本
2. スキーマ定義
3. `utterance_eval.txt`
4. LLM 評価のモック
5. 総合点算出関数
6. 発言一覧を返す API

この順で進めること。

---

# 18. 最終的な位置づけ

このプロジェクトは、
**「会議参加者の優秀さを断定するシステム」ではなく、会議の振り返りと改善を支援するシステム**
として扱うこと。

この前提が崩れる実装・表示・文言は避けること。
