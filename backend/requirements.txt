fastapi>=0.138.2
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
# openai SDK: ローカル推論バックエンド (vLLM 等) を OpenAI 互換 API で叩くために
# 使用する。OpenAI クラウド (Responses API) は蒸留・ベンチ用途の optional のみ
# (Issue #17)。
openai>=2.44.0
pyyaml>=6.0.1
python-multipart>=0.0.32
python-dotenv>=1.0.1
# フィードバック収集 DB (Issue #78)。SQLite ローカル / PostgreSQL 本番互換。
# Postgres ドライバ psycopg は optional extra [postgres] で導入する。
sqlmodel>=0.0.39
